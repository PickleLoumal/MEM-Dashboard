from django.apps import AppConfig


class AutomationConfig(AppConfig):
    """Configuration for the Automation Django app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "automation"
    verbose_name = "Automation Services"
