# 生产环境Dockerfile - 仅运行Django API服务器
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    postgresql-client \
    curl \
    && rm -rf /var/lib/apt/lists/*

# 复制requirements文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 复制项目文件
COPY . .

# 设置执行权限
RUN chmod +x scripts/run_migration.sh scripts/start_django.sh

# 暴露端口
EXPOSE 8000

# 启动Django服务器(使用启动脚本)
CMD ["/bin/bash", "/app/scripts/start_django.sh"]

